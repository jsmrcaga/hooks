name: Test

on:
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    name: 'Test'

    steps:
      - name: Get Code
        uses: actions/checkout@master

      - name: Install deps
        run: npm i

      - name: Test
        run: npm test

